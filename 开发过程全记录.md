# 🛡️ 推特百宝箱 · 开发过程全记录

> 开发日期：2026-08-17（一天完成从"脚本组装"到"v1.7.1 完善版"的全过程）
> 仓库：https://github.com/liuGuanYi-hub/x-comment-shield

---

## 一、背景与起点

用户手里有 6 段 **Twitter X Toolkit** 用户脚本代码（Config / 工具函数 / Comment Scanner / Matcher / Blocker / UI+Main 六个模块），分散在聊天记录里。目标是把它们合并成一个完整的、可安装的油猴脚本，并完成发布。

**当天起点**：`D:\zzd_project\cursor\js\文本存档\内容.txt`（纯文本代码存档）

---

## 二、开发时间线（按顺序）

### 阶段 1：脚本组装与初版发布（v1.0.0 → v1.1.0）

| 步骤 | 内容 |
|------|------|
| 1. 合并代码 | 将 6 段代码按模块顺序合并为完整 `.user.js` 脚本 |
| 2. 审查补全 | 发现"去除右侧栏"只有配置项 `hideSidebar` 没有执行逻辑 → 新增 `applySidebar()` 方法（隐藏 `[data-testid="sidebarColumn"]` + 趋势栏 + 主内容加宽 990px + MutationObserver 持续监听） |
| 3. 生成安装文件 | 产出 `推特百宝箱.user.js`（可安装版） |
| 4. Git 初始化 | `git init` → 分支改名 `main` → 添加远程 `origin` → `.gitignore` 排除 `.workbuddy/` |
| 5. 首次提交 | `f8e5f64` 新增：推特百宝箱油猴脚本 |

### 阶段 2：踩坑——GitHub 连不上（网络问题）

**现象**：`git push` 报 `fatal: unable to access ... Recv failure: Connection was reset`

**排查**：
- 检查 git 代理配置 → 无
- 探测本机代理端口 → **7897 端口有代理在监听**（Clash 类）
- `curl` 通过该端口可达 GitHub

**解决**：
```bash
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```
> 💡 经验：GitHub 直连被重置时，先查本机代理端口（7890/7897/10809 等常见），给 git 配代理即可。

### 阶段 3：按用户清单完善（v1.1.0）

- **元数据个性化**：`@author` → liuGuanYi-hub、`@namespace` → 真实仓库地址、`@description` 详细化
- **安全**：`autoBlock`（自动拉黑）开启时增加 confirm 二次确认，防账号风控
- **历史导出**：面板新增"导出历史"按钮，Blob 下载 JSON
- **错误兜底**：`scanWithScroll()` 每轮 try-catch；两个 MutationObserver 回调加错误捕获

提交：`1c8e36d`

### 阶段 4：行为调整（v1.3.0 前的多项体验优化）

| 需求 | 实现 |
|------|------|
| 只在推文详情页才扫描 | 新增 `isStatusPage()` 判断 URL 是否含 `/status/`，`scan()` 入口守卫 |
| 评论清理不自动滑动 | 移除 `scanWithScroll()` 里的 `window.scrollBy` 循环，只清当前已加载内容 |
| 盾牌图标可拖动 | Pointer Events 实现拖拽（`pointerdown/move/up` + setPointerCapture + 边界钳制 + 拖动>3px 不触发点击） |
| 面板开关图标缩放 | 打开面板 `scale(0.6)`，关闭恢复 `scale(1)`，加 `transition: transform .2s` |
| 隐藏右侧栏可选 | 默认值 `hideSidebar: false`，面板标签改为"隐藏右侧栏" |

### 阶段 5：自动更新链路（v1.2.0 → v1.3.0）

- 元数据新增 `@updateURL` / `@downloadURL` 指向 GitHub raw 链接（中文路径 URL 编码）
- **踩坑 1**：油猴显示"暂时没有更新"→ 原因①未 push（GitHub 还是旧版）；原因②已安装脚本元数据里没 updateURL（必须重装一次才生效）
- **踩坑 2**：版本号不升，油猴永远不提示更新 → 立下规范：**每次改代码必须同步递增 `@version`**
- **踩坑 3**：`raw.githubusercontent.com` 有 CDN 缓存延迟（几分钟），push 后立刻验证会拿到旧内容

### 阶段 6：UI 大改造（v1.4.0 → v1.6.3）

| 改动 | 说明 |
|------|------|
| 主题改浅色 | 黑底 `#111` → 白底 `#ffffff`，深色文字，X 蓝色按钮 |
| 最小化按钮 | 面板标题右侧 `−` 按钮 |
| 左侧栏可折叠 | 新增 `collapseLeftSidebar` 配置 + `applyLeftSidebar()` 方法（隐藏 `header[role="banner"]`） |
| 内容居中 | `setProperty("width", "990px", "important")` 强制覆盖 X 内联样式 + `translateX(calc(5vw + 140px))` 视觉微调 |
| 面板可拖动 | 标题栏 Pointer Events 拖拽，`e.target.closest("button")` 跳过按钮防冲突 |
| **修复：最小化按钮不生效** | ① openPanel HTML 漏插 `txtool-header` div → 补上；② `closePanel()` 方法漏定义 → 补上；③ 拖拽 `setPointerCapture` 劫持按钮点击 → 加 `closest("button")` 判断 |

> 💡 教训：多次用 bash `-e` 执行多步 Node 脚本时，其中一步 anchor 失败会导致后续步骤静默跳过，且脚本还会报"完成"——之后改为**写脚本文件再执行**，每步独立验证。

### 阶段 7：项目文档（v1.6.3）

- `README.md`：功能特性 / 安装 / 使用指南 / 自动更新 / 代码结构 / 许可证
- `开发计划.md`：短期（v1.7.x 稳定性）/ 中期（v1.8.x 增强）/ 长期（v2.x 平台化）/ 维护规范 / 里程碑
- `X阅读优化调研报告.md`：调研 GitHub 主流 X 清理脚本（CleanX、antfu/clean-twitter、X Reading Enhancer、TweetFilter AI、CleanYourTwitter 等）

### 阶段 8：功能板块（v1.7.0）

面板新增"功能板块"分区：
- **隐藏广告推文**（`hideAds`）：检测 Promoted / Sponsored / 推荐 / 广告 / 推广 / 赞助 → 整条隐藏
- **移除干扰区块**（`hideDistractions`）：隐藏"谁去关注"、"你可能喜欢"、趋势、"查看更多推文"

### 阶段 9：长推文截断修复（v1.7.1）

**问题**：部分含关键词的评论没被屏蔽
**排查**：不是 emoji 问题 —— X 的长评论自动截断显示（"显示更多"按钮），`tweetText.innerText` 只返回前半部分，关键词被截断匹配不上
**修复**：`scan()` 开头自动点击所有 `[data-testid="tweet-text-show-more-link"]` 展开长推文后再扫描

---

## 三、最终版本功能清单（v1.7.1）

### 评论清理
- ✅ 详情页自动扫描已加载评论（不自动滚动）
- ✅ 关键词 / 用户 / 正则三重过滤
- ✅ 自动展开长推文（修复截断误判）
- ✅ 命中评论替换为"🚫 已隐藏评论"占位块
- ✅ 历史记录本地保存（500 条）+ 查看 + JSON 导出

### 界面优化
- ✅ 浅色主题控制面板（可拖动标题栏 + 最小化按钮）
- ✅ 盾牌悬浮按钮（可拖动、点击缩放）
- ✅ 隐藏右侧栏（可选）
- ✅ 折叠左侧栏（可选，主内容居中）
- ✅ 广告徽标检测 + 干扰区块移除（功能板块）

### 工程化
- ✅ Git 版本管理（中文 commit）
- ✅ 自动更新链路（updateURL + 版本号规范）
- ✅ README / 开发计划 / 调研报告
- ✅ Node 语法检查（vm.Script）

---

## 四、提交记录

| 提交 | 说明 |
|------|------|
| `f8e5f64` | 新增：推特百宝箱油猴脚本，支持评论清理与侧栏隐藏 |
| `1c8e36d` | 优化：完善元数据、增加拉黑二次确认、历史导出与错误兜底 |
| `bfe80be` | 优化：仅推文详情页自动扫描清理，首页不再自动滚动 |
| `f27e78f` | 新增：面板打开时盾牌图标缩小，关闭时恢复 |
| `921629d` | 新增：盾牌悬浮按钮支持拖拽，拖动后不触发面板 |
| `f6ea81a` | 优化：隐藏右侧栏改为默认关闭，面板标签更明确 |
| `5c55fe8` | 新增：配置油猴更新检查链接 updateURL/downloadURL，版本升至 1.2.0 |
| `c33fec1` | 版本：升至 1.3.0（含最小化按钮、移除自动滚动、侧栏默认关闭） |
| `9cb0f24` | 新增：面板头部加最小化按钮，复用 closePanel 关闭逻辑 |
| `09b4b51` | 优化：移除自动滚动，仅清理已加载评论，滚动由用户控制 |
| `0606eeb` | 修复：openPanel HTML 缺少 txtool-header div 包裹，最小化按钮不显示 |
| `fe774f6` | 修复：补上 closePanel 方法定义，最小化按钮调用才不会报未定义 |
| `72495a6` | 优化：面板主题改为浅色（白底黑字），版本升至 1.4.0 |
| `8e5c2b5` | 优化：主内容向右偏移增加 5vw，居中效果更好 |
| `1390a32` | 修复：侧栏折叠后内容居中显示，用 !important 覆盖 X 内联样式 |
| `9120820` | 修复：侧栏折叠后内容视觉居中，加 transform translateX(140px) |
| `234d632` | 新增：面板标题栏可拖动，按钮和输入框交互不受影响，版本升至 1.6.0 |
| `8c13006` | 修复：补上面板标题栏拖拽逻辑，1.6.1 漏写绑定代码 |
| `dcdfb2a` | 修复：点击最小化按钮时不触发标题栏拖拽，避免劫持点击 |
| `13d0469` | 文档：新增 README 项目说明 |
| `246d20b` | 文档：新增开发计划路线图 |
| `c5cd25b` | 新增：功能板块-广告徽标检测与干扰区块移除，版本升至 1.7.0 |
| `b51f17e` | 修复：扫描前自动展开长推文，避免 X 截断导致关键词匹配失败 |

---

## 五、踩坑与经验总结

1. **GitHub 连接被重置** → 检查本机代理端口（7897），`git config --global http.proxy` 解决
2. **油猴不提示更新** → 三大原因：① 未 push；② 旧版元数据无 updateURL 需重装；③ **版本号必须递增**
3. **raw 链接 CDN 缓存** → push 后验证要等几分钟，或走 GitHub API 验证
4. **bash 执行多步 Node 脚本有坑** → 一步 anchor 失败后续静默跳过还报"完成"，改用脚本文件 + 每步验证
5. **X 是 SPA** → 所有 DOM 修改都要 MutationObserver 兜底，路由切换要 hookRouter
6. **X 长推文截断** → innerText 拿不全，扫描前先展开"显示更多"
7. **拖拽与按钮点击冲突** → `setPointerCapture` 会劫持子元素点击，用 `closest("button")` 跳过

---

*文档由开发过程整理，随版本持续更新。*
