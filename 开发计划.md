# 🗺️ 推特百宝箱 · 开发计划

> 版本路线图与迭代规划 —— 当前版本：1.6.3

## 一、项目现状（已完成）

| 模块 | 状态 | 说明 |
|------|------|------|
| 评论扫描 CommentScanner | ✅ 已完成 | 详情页自动扫描已加载评论，不自动滚动 |
| 规则匹配 Matcher | ✅ 已完成 | 关键词 / 用户黑名单 / 正则 三重过滤 |
| 屏蔽执行 Blocker | ✅ 已完成 | 评论替换为隐藏占位块，历史记录本地保存 |
| 控制面板 UI | ✅ 已完成 | 浅色主题、盾牌按钮可拖、面板可拖、最小化按钮 |
| 侧栏管理 | ✅ 已完成 | 右侧栏可选隐藏、左侧栏可选折叠、主内容居中 |
| 更新链路 | ✅ 已完成 | @updateURL / @downloadURL 接入 GitHub raw |
| 项目文档 | ✅ 已完成 | README、开发计划、Git 版本管理 |

## 二、短期计划（v1.7.x — 稳定性与体验）

> 目标：打磨现有功能，修复边界场景，提升日常使用舒适度。

| 优先级 | 任务 | 说明 | 验收标准 |
|--------|------|------|----------|
| P0 | 面板位置持久化 | 用 GM_setValue 记住拖动后的位置，下次打开恢复 | 拖到任意位置 → 关闭 → 重开 → 位置不变 |
| P0 | 折叠状态持久化 | 左右侧栏的折叠/隐藏状态刷新后保持 | 勾选折叠 → 刷新 → 仍折叠 |
| P1 | 屏蔽撤销机制 | 已隐藏评论支持一键"撤销隐藏"恢复显示 | 点撤销 → 评论回到原位 |
| P1 | 扫描防抖优化 | 滚动时避免频繁扫描造成卡顿 | 快速滚动页面不卡、不重复处理 |
| P2 | 导入/导出配置 | 关键词黑名单、用户黑名单支持 JSON 导入导出 | 导出 → 换电脑导入 → 配置恢复 |

## 三、中期计划（v1.8.x — 功能增强）

> 目标：扩展过滤能力，增加自定义与批量处理能力。

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 白名单机制 | 指定用户/关键词永不屏蔽，避免误伤 |
| P0 | 用户黑名单管理界面 | 面板内可视化增删用户，支持手动拉黑 |
| P1 | 批量操作 | 一键拉黑所有命中用户（带二次确认） |
| P1 | 自定义规则进阶 | 支持 @用户名 精确匹配、URL 过滤、组合规则 |
| P2 | 评论统计面板 | 显示已清理数量、今日清理、命中规则分布 |
| P2 | 通知提示优化 | 替换默认 alert，使用油猴 UI 通知（toast） |

## 四、长期计划（v2.x — 平台化）

> 目标：从个人工具走向可分享的成熟插件。

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | Greasy Fork 发布 | 提交脚本审核，公开安装渠道 |
| P0 | 多语言支持 | 面板 UI 中英文切换（i18n） |
| P1 | 配置同步 | 支持自定义同步服务或 GM 云存储 |
| P1 | 深度数据管理 | 历史记录改用 IndexedDB，突破 500 条限制 |
| P2 | 插件化规则引擎 | 规则用声明式配置（JSON Schema），支持模板市场 |

## 五、持续维护规范

### 每次改动的流程
```
1. 修改代码
2. 同步递增 @version（如 1.6.3 → 1.6.4）
3. 本地验证（Node 语法检查 + 浏览器实测）
4. git commit（中文消息）→ git push
5. 油猴自动检测更新（或手动检查）
```

### 版本号规则
| 变化类型 | 版本示例 | 说明 |
|----------|----------|------|
| 新增功能 | 1.7.0 | 次要版本号 +1 |
| Bug 修复 / 小优化 | 1.6.4 | 补丁版本号 +1 |
| 重大重构 / 平台化 | 2.0.0 | 主版本号 +1 |

### 已知问题与待验证项
- [ ] 侧栏折叠后主内容偏移量（5vw）在不同分辨率下的效果需要实测收集
- [ ] X 前端改版后选择器（sidebarColumn / banner / primaryColumn）可能失效，需定期验证
- [ ] 多个 X 相关脚本同时安装时的冲突排查流程需沉淀
- [ ] 自动更新链路端到端验证（push → CDN 刷新 → 油猴提示）还需完整走一遍

## 六、里程碑

| 里程碑 | 目标版本 | 内容 |
|--------|----------|------|
| M1 稳定可用 | 1.7.0 | 位置/状态持久化 + 屏蔽撤销 |
| M2 功能完善 | 1.8.0 | 白名单 + 批量操作 + 统计面板 |
| M3 公开发布 | 1.9.0 | Greasy Fork 上架 + 文档完善 |
| M4 平台化 | 2.0.0 | 多语言 + 规则引擎 + 同步 |

---

*计划文档随版本迭代持续更新，每次发布后回填实际进度。*
